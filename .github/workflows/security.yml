name : Security Test
on :
  push:
  pull_request:

jobs:
  security:
    runs-on: ubuntu-latest
    steps:

      - uses : actions/checkout@v4
      - name : test
        run : | echo "Hello from security checker!!"
